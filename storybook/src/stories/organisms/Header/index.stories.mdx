import { Meta } from '@storybook/addon-docs';

import 'mdfin-header/dist/css/Header.css';
import scss from './Header.module.scss';

import { storytitle, args } from './props';

import { Header } from 'mdfin-header';
import { MainMenu, ALIGNMENT } from 'mdfin-main-menu';
import { Logo } from 'mdfin-logo';

<Meta title={`${storytitle}/Description`} />

# Header

<div className="description">This is the description of the component.</div>

**Directory** `components/Header`

### Desktop header

<div className={scss.decorator}>
  <Header
    Logo={<Logo value={'/Logo/logo-imprumut-acum.webp'} className={scss.img} />}
    MainMenu={<MainMenu items={args.items} alignment={args.alignment} />}
    className={scss.header}
  />
</div>

### Device header

<div className={scss.decorator}>
  <Header
    Logo={<Logo value={'/Logo/logo-imprumut-acum.webp'} className={scss.img} />}
    MainMenu={
      <MainMenu
        items={args.items}
        isDevice={true}
        alignment={ALIGNMENT.RIGHT}
      />
    }
    className={scss.header}
  />
</div>

[**component-driven**](/story/organisms-header--primary-header)
